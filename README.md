# How to Copy the Config Files
* the command is: `sudo cp -R root/* /`
* this will recursively copy every config file where it needs to go
* be careful, though, or else you may really mess things up!
